monolog:
    handlers:
        # Not `type: stream` -- the stream handler is wrapped by
        # App\Log\ProbeFilterHandler, which drops the /healthz probe lines.
        main:
            type: service
            id: App\Log\ProbeFilterHandler

when@test:
    monolog:
        handlers:
            main:
                type: test
                level: debug
